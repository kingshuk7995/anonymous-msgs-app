<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Dashboard</title>
  <style>
    * {
      margin: 9px;
      padding: 0px;
      box-sizing: border-box;
    }
    body {
      background-color: #000;
      color: #fff;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      padding: 30px;
    }

    h1 {
      color: aqua;
      text-shadow: 0 0 10px #0ff, 0 0 20px #0ff;
    }

    .link-box {
      background-color: #11065a;
      padding: 20px;
      border-radius: 12px;
      box-shadow: 0 0 10px rgba(0, 255, 255, 0.2);
      margin: 20px 0;
    }

    .msg-list {
      margin-top: 30px;
    }

    .msg {
      background-color: #1e1e1e;
      padding: 15px;
      border-radius: 8px;
      margin-bottom: 10px;
      box-shadow: inset 0 0 5px rgba(0, 255, 255, 0.3);
    }

    a.share-link {
      color: #0ff;
      text-decoration: underline;
      font-size: 16px;
    }
  </style>
</head>
<body>
  <h1>Welcome, <%= user.name %>!</h1>

  <div class="link-box">
    <p>Share your message link:</p>
    <a class="share-link" href="/send/<%= user._id %>" target="_blank">
      http://localhost:3000/send/<%= user._id %>
    </a>
  </div>

  <div class="msg-list">
    <h2>Your Messages:</h2>
    <% if (user.messages.length === 0) { %>
      <p>No messages yet.</p>
    <% } else { %>
      <% user.messages.forEach(msg => { %>
        <div class="msg"><%= msg.text %></div>
      <% }); %>
    <% } %>
  </div>
</body>
</html>
